import { CreativeRouteLoader } from '@/components/ui/CreativeRouteLoader';

export default function SolutionsLoading() {
  return (
    <CreativeRouteLoader
      label="Solutions Matrix"
      title="Assembling your outcome blueprint"
      hint="Preparing solution systems, process flow, and matched plan recommendations."
    />
  );
}
